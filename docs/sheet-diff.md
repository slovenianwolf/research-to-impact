# Sheet diff — live "[MASTER HUB] IGNITE Public Goods Website" → current site structure

Generated by reading the live sheet (read-only) and comparing to what the build now
expects. This is the **authoritative** to-do (it supersedes the generic steps in
`sheet-sync.md` where they differ, because it reflects your actual current content).

Good news: your live content is well-organized and **nothing needs to be deleted**.
The changes are additions, five Site edits, and a few **relocations** you should
decide on.

---

## A. Site tab — 5 edits + 4 new rows

**Change these `value` cells:**

| setting | change to |
| --- | --- |
| tagline | Field-built practices for closing gaps — free for every school. |
| hero_headline | Practices that help every student reach grade-level work — built with a focus on students with disabilities. |
| hero_subhead | Co-planning, repeated reading, and routine data cycles: classroom practices developed with schools to better serve students with disabilities and others furthest from opportunity. Free guides, templates, and videos to put them to work. |
| license_default | CC BY 4.0 |
| license_line | © 2026 Marshall Street at Summit Public Schools. Shared with permission from partner schools. Free to use, share, and adapt with attribution. (CC BY 4.0) |

**Add these rows** (`setting`, `value`):

| setting | value |
| --- | --- |
| about_network | *(see fixtures/Site.csv for the drafted text — confirm public-naming clearance + the exact Gates acknowledgment wording)* |
| hubspot_portal_id | *(blank until you have it)* |
| hubspot_form_id | *(blank until you have it)* |
| hubspot_region | na1 |

> Note: the license change is also why the **Column Guide** tab and the in-sheet
> "LICENSE" instruction (which still say CC BY-NC-ND) should be updated to **CC BY 4.0**
> so editors aren't misled. The Column Guide is reference-only — the build ignores it —
> but keep it accurate.

## B. Add a `gated` column to all 6 resource tabs

On `Co-Planning`, `Repeated Reading`, `Routine Data Cycles`,
`Leading Implementation`, `R2I Library` (the lit-/collab-/mtss-/post-/emot- tab), and
`Site Assets`: add a column header **`gated`** at the far right. Leave blank for the
toolkits; set `TRUE` only on research/funder downloads you want behind the soft email
ask (e.g., the white papers and evaluation, once relocated — see D).

## C. Sections tab — add 2 rows

`header_image` already exists as a column, so this is just two new rows:

| section_id | section_name | header_label | tier | order | nav_visible | landing_intro |
| --- | --- | --- | --- | --- | --- | --- |
| stories-spotlights | Stories & Spotlights | Stories & Spotlights | **Stories** | 7 | yes | *(from fixtures/Sections.csv)* |
| evidence-impact | Evidence & Impact | Evidence & Impact | **Evidence** | 8 | yes | *(from fixtures/Sections.csv)* |

`Stories` and `Evidence` are new `tier` values — they make these sections browsable +
in the nav but kept off the homepage (the practitioner-first rule). Update the Column
Guide's `tier` description to list them.

## D. Modules tab — add 7 rows

| section | module_order | module_name |
| --- | --- | --- |
| Stories & Spotlights | 1 | Partner Spotlights |
| Stories & Spotlights | 2 | School Case Studies |
| Stories & Spotlights | 3 | From the Field |
| Evidence & Impact | 1 | White Papers |
| Evidence & Impact | 2 | External Evaluation |
| Evidence & Impact | 3 | Annual Reports |
| Evidence & Impact | 4 | Equity-Gap Roadmap |

## E. Create the two new resource tabs

**`Stories & Spotlights`** — import `docs/sheet-templates/Stories & Spotlights.csv`
(File → Import → Insert new sheet). Note its extra `practice` column (ties a case
study to a toolkit so it also shows on that toolkit's page). All rows are unpublished
starters.

**`Evidence & Impact`** — import `docs/sheet-templates/Evidence & Impact.csv`. **This
template now contains only the net-new items** (annual reports, roadmap, change
packages) — because the white papers and evaluation already exist in your sheet and
should be *moved*, not duplicated. See D-relocations next.

---

## Decisions to make (content relocations)

These are judgment calls — I did **not** assume them. The build works either way; this
is about where things live for the reader.

### D1. Move the evidence items out of Leading Implementation → Evidence & Impact ✅ DECIDED — do this

Your `Leading Implementation` tab currently holds these in the **Core Content** module:

| id (keep it — ids are permanent) | move to Evidence & Impact module |
| --- | --- |
| li-1-year-1-white-paper-from-innovation-to-infrastruc | White Papers |
| li-1-year-2-white-paper | White Papers |
| li-1-evaluation-report | External Evaluation |

To move: cut the row from the `Leading Implementation` tab, paste it onto the
`Evidence & Impact` tab, and change its `module` cell to the new module. **Keep the id
and the `published` value** (the Year 1 paper stays live). This gives the research a
clean, demoted home and de-clutters the practitioner-facing Leading Implementation
section. Set `gated = TRUE` on these if you want the soft email ask.

*Leave in Leading Implementation* (they're implementation-facing, not evidence):
the Partner Handbook, Executive Summary 2-pager, Visual Improvement Pathway, Spread &
Scale tool, checklists, training decks. (The Executive Summary could go either way —
your call.)

### D2. Stories: keep practice case studies in-context; aggregate the rest (recommended)

Your toolkits already have case studies *in context* (e.g. `cp-2-case-study-1-...`,
`rr-4-case-study-2-...`, `rdc-4-case-study-2-...`) — that's good, leave them. The new
`Stories & Spotlights` section is for **partner spotlights**, **cross-cutting case
studies**, and **talks**. The two generic case studies under Leading Implementation's
`Stories` module (`li-4-case-study-1`, `li-4-case-study-2`) are good candidates to
move to `Stories & Spotlights → School Case Studies`. Optional: add a `practice` tag
to any toolkit case study you also want surfaced in the aggregated Stories index.

---

## F. Dropdowns + verify

- Add the data-validation dropdowns from the [Editor's Guide](editor-guide.md) —
  include the new `gated` (TRUE/FALSE), `practice`, and the `Stories`/`Evidence` tier
  values.
- Keep the sheet shared **Anyone with the link → Viewer**.
- Trigger a rebuild (**Actions → Scheduled rebuild → Run workflow**) and read the
  report. Target **"No warnings."** New tabs should appear under "Tabs read" and the
  two new sections under "nav sections."
